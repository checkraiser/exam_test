<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<% if courses.length > 0 %>
	<ul>
		<% courses.each do |course| %>
			<li><%= link_to course.name, course_path(course.id) %>
		<% end %>
	</ul>
<% else %>
	You have no course
<% end %>
